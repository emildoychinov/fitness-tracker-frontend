export enum RouterPathEnum {
    HOME = '/',
    ABOUT = '/about',
    MEMBER = '/member',
    LOGIN = "/login",
    REGISTER = '/register',
    SEARCH = '/search/:search_text',
    WORKOUT = "/workouts/:id"
}